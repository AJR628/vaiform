entrypoint = "server.js"
modules = ["nodejs-22"]
hidden = [".config", "package-lock.json"]

# This is what the green Run button executes: API runtime + finalize worker runtime
run = "node scripts/start-replit-runtime.mjs"

[nix]
channel = "stable-24_11"
packages = ["openssh", "stripe-cli"]

# Deploy config isn't needed for local Run. Remove to avoid confusion.
# [deployment]
# run = ["node", "server.js"]
# deploymentTarget = "autoscale"
# ignorePorts = false

# Only expose your app once
[[ports]]
localPort = 3000
externalPort = 80

[agent]
expertMode = true
