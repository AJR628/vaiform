Greenlight: Implement Backend Audit Phase 1 — Commit 1 only.

Use BACKEND_AUDIT_PHASE1_PLAN.md as the source of truth, specifically:
“Commit 1 — Remove placeholder Netlify origin from CORS allow-list.”

Do not implement Commit 2, Commit 3, Commit 4, or Commit 5.
Do not make opportunistic cleanup.
Do not reformat unrelated lines.
Do not modify docs.
Do not modify tests.
Do not modify any file except src/app.js.

Task:
Remove the placeholder Netlify origin from the ALLOWED_ORIGINS array in src/app.js.

Remove only this entry and its trailing comment:

'https://vaiform-user-name.netlify.app', // replace with your actual Netlify preview URL if used

Preserve all surrounding entries and behavior exactly:
- 'https://vaiform.com'
- 'https://www.vaiform.com', // www subdomain
- 'http://localhost:3000'
- 'http://localhost:8888', // local development
- the isReplitPreview dev-only wildcard logic
- corsOptions behavior
- Helmet/CSP settings
- all route mounting
- all imports
- all logging

Required pre-change verification:
1. Run:
   rg -n 'vaiform-user-name' src/

2. Expected result:
   Exactly one match in src/app.js.

3. Read the ALLOWED_ORIGINS section in src/app.js and confirm the entry is the placeholder described in the Phase 1 plan.

Stop before editing if:
- The grep returns zero matches.
- The grep returns more than one match.
- The match appears outside src/app.js.
- The surrounding CORS code differs from the Commit 1 plan.

Implementation:
Make exactly one logical change:
Remove the placeholder origin entry from ALLOWED_ORIGINS.

Required post-change verification:
1. Run:
   rg -n 'vaiform-user-name' src/

   Expected: zero matches.

2. Run:
   npm run lint

   Expected: green.

3. Run the smallest available app import/server smoke for this repo. Prefer:
   node -e "import('./src/app.js').then(()=>console.log('ok'))"

   If this repo has a different established import smoke, use that and report the command.

4. Show:
   git diff -- src/app.js

   Expected:
   Only the placeholder origin line removed. No unrelated formatting or behavior changes.

5. Show:
   git status --short

   Expected:
   Only src/app.js changed.

Commit:
If and only if every verification step passes, create one commit with this message:

chore(cors): remove placeholder Netlify origin from allow-list

Final report:
Return:
1. Pre-check result
2. Files changed
3. Diff summary
4. Verification commands and results
5. Commit hash
6. Final git status
7. Any deviations or concerns

Mandatory stop condition:
If any verification step fails or the diff includes anything beyond the placeholder origin removal, do not commit. Stop and report exactly what happened.

Do not proceed to Commit 2.