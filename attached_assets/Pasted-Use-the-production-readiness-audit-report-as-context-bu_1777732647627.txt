Use the production readiness audit report as context, but do not trust it blindly.

Your task is to create a verified Backend Audit Phase 1 commit plan.

Do not modify files.
Do not implement changes.
Do not run destructive commands.
Do not make assumptions.
Do not rely only on the previous audit report.
You must independently verify every proposed change against the current repository.

Goal:
Create a narrow, evidence-backed implementation plan for the highest-value backend fixes before paid beta.

Allowed focus areas only:
1. src/utils/image.fetch.js SSRF/redirect safety
2. src/controllers/assets.controller.js req.session / req.isPro logic
3. /api/limits or related limits controller usage drift
4. Dead or legacy health controller cleanup, only if proven unused
5. Conservative backend rate limiting
6. Placeholder/stale CORS origin cleanup
7. Stripe webhook identity hardening plan, but only if repo evidence proves the current risk and the change is low-risk

Hard exclusions:
- Do not refactor story.service.js.
- Do not refactor story.routes.js.
- Do not refactor caption.preview.routes.js.
- Do not change finalize/render behavior.
- Do not change worker behavior.
- Do not change billing behavior without tests and explicit approval.
- Do not add Firestore-backed asset dedupe.
- Do not introduce new architecture.
- Do not create broad cleanup commits.
- Do not touch unrelated files.

Verification requirements:
For every proposed change, first run repo inspection using grep/search/read commands as needed.

You must prove:
1. The current behavior exists.
2. The affected route/helper/service is active or inactive.
3. The proposed fix has a clear target.
4. The proposed fix does not require hidden assumptions.
5. The proposed fix has a verification method.
6. The proposed fix can be rolled back cleanly.
7. Any docs affected are identified.

For each candidate issue, include:
- Finding title
- Whether verified: yes/no/inconclusive
- Exact file paths and line references
- Current behavior
- Why it matters
- Whether it is pre-beta blocking
- Minimal safe fix
- Tests needed
- Verification commands
- Risk level
- Rollback plan
- Whether docs need updating

Required output:

A. Verified findings summary
Group into:
- Confirmed and should fix now
- Confirmed but should defer
- Inconclusive, needs more investigation
- Not valid / do not act on

B. Backend Audit Phase 1 commit plan
Create small commits only.

For each commit:
- Commit title
- Goal
- Files expected to change
- Files explicitly not to touch
- Evidence supporting the change
- Exact intended behavior change
- Test additions or updates
- Verification commands
- Manual test steps if needed
- Risk level
- Rollback notes

C. Recommended order
Explain why the commits should happen in that order.

D. Stop conditions
List conditions where implementation should stop and ask for human review.

E. Commit 1 implementation prompt only
At the end, write a separate prompt for implementing Commit 1 only.
Do not implement Commit 1 yet.

Important:
Be extremely conservative. The goal is not to make the repo prettier. The goal is to reduce real beta risk with minimal, evidence-backed changes.