Housekeeping before Backend Audit Phase 1 Commit 2.

Do not implement Commit 2 yet.
Do not modify source code.
Do not modify tests.
Do not modify docs unrelated to BACKEND_AUDIT_PHASE1_PLAN.md.
Do not touch src/app.js.

Context:
Backend Audit Phase 1 Commit 1 has been implemented and pushed as:

1c0b46f86009c3b0797d2974255ebe725b554993

Commit 1 correctly removed the placeholder CORS origin from src/app.js, but it also accidentally committed an attached prompt file:

attached_assets/Pasted-Greenlight-Implement-Backend-Audit-Phase-1-Commit-1-onl_1777734793695.txt

Task:
Create one housekeeping commit that:
1. Deletes only that accidental attached_assets/Pasted-* prompt file.
2. Updates BACKEND_AUDIT_PHASE1_PLAN.md to show Commit 1 is complete.

Required BACKEND_AUDIT_PHASE1_PLAN.md updates:
1. Change the top status from read-only/no commits to something like:

Status: Phase 1 implementation in progress.
Commit 1 complete: 1c0b46f86009c3b0797d2974255ebe725b554993.
Commits 2–5 pending explicit greenlight, one at a time.

2. Add a short “Implementation progress” section near the top:

## Implementation progress

| Commit | Status | Commit SHA | Notes |
|---|---|---|---|
| Commit 1 — Remove placeholder Netlify origin from CORS allow-list | Complete | 1c0b46f86009c3b0797d2974255ebe725b554993 | Removed only the placeholder CORS origin from src/app.js. Verification passed: rg zero matches, lint green with pre-existing warnings, app import smoke green. |
| Commit 2 — Delete src/controllers/health.controller.js | Pending | — | Awaiting explicit greenlight. |
| Commit 3 — Remove /api/limits route | Pending | — | Awaiting explicit greenlight and caller re-check. |
| Commit 4 — Fix assets.controller.js req.session/req.isPro no-ops | Pending | — | Awaiting explicit greenlight. |
| Commit 5 — Delete or fix orphaned image.fetch.js | Pending | — | Awaiting explicit greenlight. |

3. Leave the original Commit 1 plan details in place for audit history, but add one line under the Commit 1 heading:

Implementation status: Complete in 1c0b46f86009c3b0797d2974255ebe725b554993.

4. Do not rewrite or reformat the full plan.
5. Do not change Commit 2–5 scopes.

Pre-check:
1. Run:
   git status --short

2. Confirm whether the accidental attached_assets/Pasted-* file exists.

3. Confirm:
   rg -n 'vaiform-user-name' src/
   Expected: zero matches.

Implementation:
- Delete only the accidental attached_assets/Pasted-* prompt file.
- Update only BACKEND_AUDIT_PHASE1_PLAN.md as described above.

Post-check:
1. Run:
   git diff --stat

   Expected:
   Only:
   - one deleted attached_assets/Pasted-* file
   - BACKEND_AUDIT_PHASE1_PLAN.md modified

2. Run:
   git diff -- BACKEND_AUDIT_PHASE1_PLAN.md

   Expected:
   Only status/progress updates. No scope changes to Commits 2–5.

3. Run:
   rg -n 'vaiform-user-name' src/

   Expected:
   zero matches.

4. Run:
   npm run lint

   Expected:
   green, allowing pre-existing warnings only.

Commit message:
docs(audit): mark backend phase 1 commit 1 complete

Final report:
- Files changed
- Deleted attachment filename
- Plan updates made
- Verification results
- Commit hash
- Final git status

Stop after this housekeeping commit. Do not proceed to Commit 2.