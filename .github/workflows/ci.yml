name: CI (hello)

on:
  push:
  pull_request:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install deps (best effort)
        run: npm ci || npm i
      - name: Smoke
        run: node -e "console.log('✅ CI is wired up')"
